extend type Query {
    question(id: ID! @eq): Question @guard(with: ["api"]) @find 
    questions(
        orderBy: _ @orderBy(columns: ["created_at"])
        filter: String
    ): [Question!]! @guard(with: ["api"]) @paginate(builder: "App\\GraphQL\\Queries\\Questions")
}
