extend type Mutation {
    addMember(user_id: ID!, team_role_id: ID, team_id: ID!): Member!
    updateMember(id: ID!, user_id: ID!, team_role_id: ID, team_id: ID!): Member!
    deleteMember(id: ID!): Member!
}
