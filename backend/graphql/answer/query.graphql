input filter {
    user_id: ID
}

extend type Query {
    answer(id: ID! @eq): Answer @guard(with: ["api"]) @find
    answers(filter: filter @spread): [Answer!]!
        @guard(with: ["api"])
        @paginate(builder: "App\\GraphQL\\Queries\\Answers")
}
